# CLAUDE.md

## 项目信息

* 远端项目主目录：`请填写真实远端路径`
* 除远端项目主目录外，所有路径均使用基于当前主文件夹的相对路径。
* 提供远端测试命令时，必须先进入本文件指定的远端项目主目录，不使用占位路径。

## 工作方式

Codex 应把每个任务视为一个独立、可验证的工作单元。开始复杂任务前，先阅读相关文件、确认目标范围，并给出简洁计划。实现过程中保持改动集中，避免顺手重构无关代码。

任务输入通常应包含目标、上下文、约束和完成标准。若用户没有给全，Codex 应基于现有信息推进；只有缺少关键信息且会影响正确性时才提问。

## 计算资源

* 不要在本地运行 GPU 训练、大规模推理或其他高计算量程序。
* 需要复杂计算时，修改代码并提交后，由用户将代码推送或拉取到远端 GPU 服务器运行。
* 本地只运行轻量级检查，例如格式化、静态检查、小规模单元测试或快速脚本验证。

## 本地修改保护

Codex 绝不能随意丢弃用户的本地修改。

遇到未提交修改时，必须先执行：

```bash
git status
git diff
```

然后向用户说明当前修改内容和风险，并询问处理方式。

禁止使用以下破坏性命令，除非用户明确要求：

```bash
git checkout -- .
git clean -fd
git reset --hard
```

## 分支管理

* 功能分支合并必须在远端服务器验证通过后进行。
* Codex 不主动合并功能分支到 `main`。
* 合并完成后，应立即删除本地和远程功能分支。
* 分支删除前必须确认相关 commit 已进入 `main` 历史。

## Git 提交规范

提交前必须检查状态：

```bash
git status
```

只添加具名文件，禁止使用：

```bash
git add .
git add -A
```

提交信息使用 conventional commits，中文为主：

```bash
feat: 添加训练配置校验
fix: 修复数据路径解析错误
refactor: 简化模型初始化逻辑
docs: 更新远端测试流程
test: 补充动作预测单元测试
chore: 调整开发脚本
```

禁止添加 `Co-Authored-By`。

## 修改、测试与实验记录

- 每次代码、脚本、配置、实验设置或排查结论发生实质修改后，必须同步更新 `docs/` 中的对应文档；没有合适文档时，新建当天 topic 文档。
- 每次训练、评估、调试实验或关键测试后，将结论记录到 `docs/`。
- 文档路径采用 `docs/YY-MM-DD/YY-MM-DD-<topic>.md`，例如 `docs/26-05-03/26-05-03-libero-joint-debug.md`。
- 记录至少包含：改动/实验目的、分支/commit、运行位置、关键命令或脚本、checkpoint/output/log 路径、主要指标、异常现象、当前结论和下一步。
- 正在运行但尚未完成的测试也要记录，状态写为“进行中”，并给出 tmux session、日志路径和后续需要读取的结果文件。
- 最终回复前自检：如果本次有代码、脚本或实验配置修改，必须确认对应 `docs/YY-MM-DD/` 文档已同步；若确实无需记录，在最终回复中说明原因。
- 不把大日志、checkpoint、dataset 或生成视频写入文档；只记录仓库内相对路径和必要摘要。

## Git 推送规范

每次 commit 后自动推送到远端，无需等待用户确认。

推送前必须先同步远端状态：

```bash
git fetch origin
```

若当前分支已有上游分支：

```bash
git push
```

若是新分支：

```bash
git push -u origin <branch-name>
```

推送前如发现冲突、落后或远端历史变化，应停止推送并向用户说明。

## 代码修改权限

* 纯文档更新可以直接修改、提交并推送，例如 `AGENTS.md`、`README.md`。
* 涉及代码逻辑、训练流程、模型结构、数据处理或测试行为的修改，提交前必须获得用户确认。
* 小范围修复也应展示变更摘要和验证结果。

## 远端服务器验证流程

每次完成代码修改并 push 后，Codex 必须提供完整远端验证说明，包括：

1. 进入项目主目录的命令；
2. 拉取最新代码的命令；
3. 如需切换分支，给出切换分支命令；
4. 运行或测试命令；
5. 成功时应看到的预期输出。

标准格式如下：

```bash
cd <本文件中指定的真实远端项目主目录>

git fetch origin
git checkout <branch-name>
git pull origin <branch-name>

# 运行 Codex 根据当前任务提供的具体测试命令
```

实际回复中不得保留 `<本文件中指定的真实远端项目主目录>` 或其他占位路径，必须替换为本文件记录的真实路径。

## 测试与验证

Codex 完成修改后应尽量执行可负担的本地验证，包括：

* 相关单元测试；
* lint、format 或 type check；
* 最小复现脚本；
* 配置解析检查；
* 文档链接和命令检查。

无法本地运行的测试，应在远端进行测试，如果仍然不行，则需要说明原因。

验证结果必须包含：

* 实际执行的命令；
* 是否通过；
* 关键输出或失败信息；
* 未验证项目及原因。

## 文档规范

* 项目文档使用中文撰写。
* 代码注释以中文为主。
* README、AGENTS.md、开发说明应保持简洁、可执行、易维护。
* 新增规则应来自实际工作流需要，避免堆叠泛化原则。
* 当 Codex 在同类问题上重复犯错，应总结原因并更新 AGENTS.md 或相关流程文档。

## Codex 使用建议

复杂任务优先使用计划模式。计划应覆盖目标、相关文件、实现步骤、验证方式和风险点。计划通过后再进入实现。

长任务应保持一个会话对应一个明确工作单元。若任务方向发生分叉，应开启新会话或新分支，避免上下文膨胀。

重复性流程应沉淀为 Skill。稳定、周期性流程可进一步设置 Automation。Skill 负责方法，Automation 负责调度。

## MCP 与外部上下文

当任务依赖仓库外部信息，例如 issue、PR、CI、设计文档、任务系统或云端文件，应优先使用已配置的 MCP 或连接器获取上下文。

只添加能解决真实工作流问题的 MCP。避免为单次任务引入长期维护成本。

## 完成标准

一个任务完成时，应满足：

* 目标功能或文档修改已完成；
* 变更范围集中；
* 未破坏用户本地修改；
* 已按规范提交和推送；
* 已提供验证结果；
* 需要远端运行时，已给出可直接执行的远端命令和预期输出。
